# Sprint-coverage-calculator / AI Compatibility Context

Current repository authority lives in `AGENTS.md` and `.ai/`.

This file is retained only for tools that still index `docs/ai/`. It must not define current governance or developer-agent provider selection.

Repository-specific context lives in `.ai/repo/context.md`.
